{
  "name": "@onivoro/server-parameterization",
  "version": "0.5.0",
  "repository": {
    "url": "https://github.com/onivoro/server-parameterization.git"
  },
  "scripts": {
    "test": "jest",
    "build": "tsc -p tsconfig.json",
    "release": "rm -rf dist && npm run build && npm version minor && cp package.json dist && cd dist && npm publish --scope public"
  },
  "devDependencies": {
    "@types/jest": "^26.0.14",
    "@types/node": "^14.11.2",
    "jest": "^26.4.2",
    "ts-jest": "^26.4.1",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "@nestjs/common": "^7.6.3",
    "minimist": "^1.2.5",
    "reflect-metadata": "^0.1.13",
    "tslib": "^2.0.1"
  }
}
